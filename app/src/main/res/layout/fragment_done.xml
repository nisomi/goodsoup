<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/randomImgView"
        android:layout_marginBottom="60dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="200dp"
        android:layout_height="200dp" />

    <Button
        android:id="@+id/goBackBtn"
        android:layout_marginTop="10dp"
        app:layout_constraintStart_toStartOf="@id/randomImgView"
        app:layout_constraintEnd_toEndOf="@id/randomImgView"
        app:layout_constraintTop_toBottomOf="@id/randomImgView"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:text="Ура, ти приготував супчик" />

</androidx.constraintlayout.widget.ConstraintLayout>